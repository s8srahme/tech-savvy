import { RouteObjectType } from "@/router/router.types";

import { AuthRoutePath } from "./auth.routes";

// Auth-level route object types

export type AuthRouteMapping = RouteObjectType<AuthRoutePath>;
export type AuthRouteObject = RouteObjectType<AuthRoutePath>;
