/**
 * The arrays below are initialized with an array-like object carrying undefined on each position (value of _).
 *
 * export const CARDS: CardProps[] = Array.from({ length: 10 }, (_, index) => ({ ...props, id: index.toString() }));
 * export const CAROUSEL_CARDS: CardProps[] = Array.from(Array(10), (_, index) => ({ ...props, id: index.toString() }));
 */
